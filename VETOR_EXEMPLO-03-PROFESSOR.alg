algoritmo "MEDIA"

var
   nota, i, media : inteiro // números digitados
   meuvetor : vetor [0..4] de inteiro // nome do vetor / tamanho do vetor / tipo de dados real

inicio

      escreval ("Digite o valor")
      para i de 0 ate 4 faca
      leia ( meuvetor[i] )
      nota <- nota + meuvetor[i]// condição

      fimpara

       // Seção de Comandos
fimalgoritmo
